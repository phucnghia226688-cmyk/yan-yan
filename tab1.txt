                {activeDetailTab === 'info' && (
                  <div className="space-y-4">
                    {/* Gia Hạn Banner */}
                    <div className="p-4 bg-gradient-to-r from-indigo-50/90 to-amber-50/90 border border-indigo-200/80 rounded-2xl flex flex-col sm:flex-row sm:items-center justify-between gap-3 shadow-2xs">
                      <div>
                        <p className="text-xs font-black text-indigo-950 uppercase tracking-wide flex items-center gap-1.5">
                          <RotateCcw className="w-4 h-4 text-[#FF4E00]" /> Gia Hạn Mới Gói Tập & Cập Nhật Doanh Thu
                        </p>
                        <p className="text-xs text-slate-600 font-medium mt-0.5">
                          Thêm số buổi gói mới, tự động ghi nhận doanh thu & lợi nhuận vào tháng {new Date().getMonth() + 1}/{new Date().getFullYear()}, tùy chọn điều chỉnh hạn HĐ.
                        </p>
                      </div>
                      <button
                        onClick={() => openRenewModal(selectedClient)}
                        className="bg-[#FF4E00] hover:bg-orange-600 text-white font-extrabold px-4 py-2 rounded-xl text-xs shadow-md transition-all whitespace-nowrap self-start sm:self-auto active:scale-95 flex items-center gap-1.5"
                      >
                        <RotateCcw className="w-3.5 h-3.5" />
                        Gia Hạn Ngay
                      </button>
                    </div>

                    <div className="grid grid-cols-2 sm:grid-cols-2 lg:grid-cols-4 gap-3">
                      <div className="p-4 bg-white border-2 border-indigo-100/90 rounded-2xl shadow-2xs relative group hover:border-indigo-300 transition-all">
                        <div className="flex items-center justify-between">
                          <p className="text-xs font-bold text-slate-500 uppercase">Gói tập & Số buổi còn lại</p>
                          <button
                            onClick={() => openEditModal(selectedClient)}
                            className="p-1 rounded-lg text-slate-400 hover:text-indigo-600 hover:bg-indigo-50 transition-all cursor-pointer"
                            title="Chỉnh sửa gói tập & số buổi"
                          >
                            <Edit3 className="w-4 h-4 text-slate-500 hover:text-indigo-600" />
                          </button>
                        </div>
                        <p className="text-sm font-extrabold text-indigo-900 mt-1">
                          {selectedClient.packageName || (selectedClient.clientType === 'monthly' ? 'Gói Khách Tháng' : 'Gói PT')}
                        </p>
                        {selectedClient.clientType === 'monthly' ? (
                          <p className="text-sm font-black text-amber-900 mt-0.5">
                            📅 Thẻ Khách Tháng (Đến {selectedClient.endDate || '---'})
                          </p>
                        ) : (
                          <p className="text-lg font-black text-emerald-600 mt-0.5">
                            {selectedClient.remainingSessions} / {selectedClient.totalSessions} buổi
                          </p>
                        )}
                      </div>
                      <div className="p-4 bg-white border-2 border-indigo-100/90 rounded-2xl shadow-2xs hover:border-indigo-300 transition-all">
                        <p className="text-xs font-bold text-slate-500 uppercase">Check-in gần nhất</p>
                        <p className="text-sm font-black text-indigo-600 mt-1">
                          {clientCheckIns.length > 0
                            ? new Date(clientCheckIns[0].timestamp).toLocaleString('vi-VN', {
                                day: '2-digit',
                                month: '2-digit',
                                year: 'numeric',
                                hour: '2-digit',
                                minute: '2-digit'
                              })
                            : 'Chưa check-in'}
                        </p>
                      </div>
                      <div className="p-4 bg-white border-2 border-indigo-100/90 rounded-2xl shadow-2xs relative group hover:border-indigo-300 transition-all">
                        <div className="flex items-center justify-between">
                          <p className="text-xs font-bold text-slate-500 uppercase">Ngày bắt đầu - Hạn HĐ</p>
                          <button
                            onClick={() => openEditModal(selectedClient)}
                            className="p-1 rounded-lg text-slate-400 hover:text-indigo-600 hover:bg-indigo-50 transition-all cursor-pointer"
                            title="Chỉnh sửa thời hạn hợp đồng"
                          >
                            <Edit3 className="w-4 h-4 text-slate-500 hover:text-indigo-600" />
                          </button>
                        </div>
                        <p className="text-sm font-bold text-slate-900 mt-1 flex items-center gap-1.5 flex-wrap">
                          <span>{selectedClient.startDate}</span>
                          <span className="text-slate-400">→</span>
                          <span className="text-amber-800 dark:text-amber-300 font-extrabold bg-amber-100/80 px-2 py-0.5 rounded-md border border-amber-300/80">
                            {selectedClient.endDate}
                          </span>
                        </p>
                      </div>
                      <div className="p-4 bg-slate-50 border border-slate-200 rounded-2xl">
                        <div className="flex items-center justify-between">
                          <p className="text-xs font-bold text-slate-500 uppercase">Trạng thái hợp đồng</p>
                          <button
                            onClick={() => openEditModal(selectedClient)}
                            className="p-1 rounded-lg text-slate-400 hover:text-indigo-600 hover:bg-indigo-50 transition-all cursor-pointer"
                            title="Chỉnh sửa trạng thái hợp đồng"
                          >
                            <Edit3 className="w-4 h-4 text-slate-500 hover:text-indigo-600" />
                          </button>
                        </div>
                        <p className="text-sm font-bold text-[#FF4E00] mt-1 uppercase">
                          {selectedClient.status === 'active' ? 'ĐANG TẬP (HOẠT ĐỘNG)' : selectedClient.status === 'expiring' ? 'SẮP HẾT HẠN' : selectedClient.status === 'expired' ? 'ĐÃ HẾT HẠN' : selectedClient.status === 'paused' ? 'BẢO LƯU / TẠM DỪNG' : selectedClient.status}
                        </p>
                      </div>
                    </div>

                    <div className="p-4 bg-slate-50/70 border border-slate-200 rounded-2xl space-y-4">
                      {/* Quick Observation: Check-in History & Dates */}
                      <div>
                        <div className="flex items-center justify-between mb-2">
                          <h5 className="text-xs font-bold text-emerald-700 uppercase tracking-wider flex items-center gap-1.5">
                            <Calendar className="w-4 h-4 text-emerald-600" /> Lịch Sử & Ngày Đã Check-In ({clientCheckIns.length} lượt)
                          </h5>
                          {clientCheckIns.length > 0 && (
                            <button
                              onClick={() => setActiveDetailTab('history')}
                              className="text-[11px] text-indigo-600 hover:text-indigo-800 font-bold underline flex items-center gap-1 cursor-pointer"
                            >
                              Xem tất cả lịch sử →
                            </button>
                          )}
                        </div>

                        {clientCheckIns.length === 0 ? (
                          <p className="text-xs text-slate-500 italic bg-white p-3 rounded-xl border border-slate-200">
                            Chưa có lượt check-in nào được ghi nhận cho học viên này. Bấm "Check-in 1-Chạm" ở góc trên để ghi nhận.
                          </p>
                        ) : (
                          <div className="space-y-2.5">
                            {/* Date Badges Overview */}
                            <div className="p-3 bg-white rounded-2xl border border-slate-200 shadow-2xs">
                              <p className="text-[11px] font-bold text-slate-500 uppercase tracking-wider mb-2">
                                📅 Danh sách ngày check-in gần đây:
                              </p>
                              <div className="flex flex-wrap gap-1.5 max-h-32 overflow-y-auto pr-1">
                                {clientCheckIns.slice(0, 15).map((ci) => {
                                  const dateObj = new Date(ci.timestamp);
                                  const formattedDate = dateObj.toLocaleDateString('vi-VN', {
                                    day: '2-digit',
                                    month: '2-digit',
                                    year: 'numeric'
                                  });
                                  const formattedTime = dateObj.toLocaleTimeString('vi-VN', {
                                    hour: '2-digit',
                                    minute: '2-digit'
                                  });
                                  return (
                                    <span
                                      key={ci.id}
                                      className="inline-flex items-center gap-1 text-[11px] font-extrabold bg-emerald-50 text-emerald-800 border border-emerald-200/80 px-2.5 py-1 rounded-lg hover:bg-emerald-100 transition-colors"
                                      title={`${ci.dayPlanName || 'Buổi tập'} (${formattedTime})`}
                                    >
                                      <CheckCircle2 className="w-3 h-3 text-emerald-600 shrink-0" />
                                      {formattedDate}
                                      <span className="text-[10px] text-emerald-600 font-semibold">({formattedTime})</span>
                                    </span>
                                  );
                                })}
                                {clientCheckIns.length > 15 && (
                                  <span className="inline-flex items-center text-[11px] font-bold text-slate-500 bg-slate-100 px-2 py-1 rounded-lg border border-slate-200">
                                    +{clientCheckIns.length - 15} ngày khác...
                                  </span>
                                )}
                              </div>
                            </div>

                            {/* Mini recent 3 check-ins card list */}
                            <div className="grid grid-cols-1 sm:grid-cols-3 gap-2">
                              {clientCheckIns.slice(0, 3).map((ci) => {
                                const dateObj = new Date(ci.timestamp);
                                const formattedDateTime = dateObj.toLocaleString('vi-VN', {
                                  day: '2-digit',
                                  month: '2-digit',
                                  hour: '2-digit',
                                  minute: '2-digit'
                                });
                                return (
                                  <div key={ci.id} className="p-2.5 bg-white border border-slate-200 rounded-xl text-xs flex flex-col justify-between">
                                    <div className="flex items-center justify-between font-extrabold text-slate-900 gap-1">
                                      <span className="truncate">{ci.dayPlanName || 'Check-in buổi tập'}</span>
                                      <span className="text-[10px] bg-emerald-100 text-emerald-800 px-1.5 py-0.5 rounded-md font-bold shrink-0">
                                        Trừ 1 buổi
                                      </span>
                                    </div>
                                    <p className="text-[11px] text-slate-500 font-medium mt-1 flex items-center gap-1">
                                      <Clock className="w-3 h-3 text-slate-400 shrink-0" />
                                      {formattedDateTime}
                                    </p>
                                    {ci.notes && (
                                      <p className="text-[10px] text-indigo-600 italic font-medium mt-0.5 truncate">
                                        "{ci.notes}"
                                      </p>
                                    )}
                                  </div>
                                );
                              })}
                            </div>
                          </div>
                        )}
                      </div>

                      <div className="pt-3 border-t border-slate-200">
                        <h5 className="text-xs font-bold text-indigo-700 uppercase tracking-wider mb-2 flex items-center gap-1.5">
                          <Calendar className="w-4 h-4 text-indigo-600" /> Lịch Tập Cố Định / Linh Hoạt ({(selectedClient.preferredDays || []).length} buổi/tuần)
                        </h5>
                        {(selectedClient.preferredDays || []).length === 0 ? (
                          <p className="text-xs text-slate-500 italic">Chưa cài đặt lịch tập.</p>
                        ) : (
                          <div className="flex flex-wrap gap-2">
                            {(selectedClient.preferredDays || []).map(d => {
                              const dayNames: Record<number, string> = { 1: 'Thứ 2', 2: 'Thứ 3', 3: 'Thứ 4', 4: 'Thứ 5', 5: 'Thứ 6', 6: 'Thứ 7', 0: 'Chủ Nhật' };
                              const timeVal = (selectedClient.dayTimes && selectedClient.dayTimes[d]) || selectedClient.preferredTime || '08:00 - 09:00';
                              return (
                                <div key={d} className="flex items-center gap-1.5 bg-indigo-50 border border-indigo-200/90 px-3 py-1.5 rounded-xl text-xs font-black text-indigo-950 shadow-2xs">
                                  <span className="text-indigo-700 font-extrabold">{dayNames[d]}:</span>
                                  <span className="text-emerald-800 bg-white px-2 py-0.5 rounded-md border border-indigo-150 font-bold">{timeVal}</span>
                                </div>
                              );
                            })}
                          </div>
                        )}
                      </div>

                      <div className="pt-3 border-t border-slate-200">
                        <h5 className="text-xs font-bold text-[#FF4E00] uppercase tracking-wider mb-1 flex items-center gap-1.5">
                          <Target className="w-4 h-4 text-[#FF4E00]" /> Mục tiêu tập luyện
                        </h5>
                        <p className="text-sm text-slate-800 font-medium">{selectedClient.goals || 'Chưa ghi nhận'}</p>
                      </div>

                      <div className="pt-3 border-t border-slate-200">
                        <h5 className="text-xs font-bold text-rose-600 uppercase tracking-wider mb-1 flex items-center gap-1.5">
                          <Heart className="w-4 h-4 text-rose-500" /> Ghi chú sức khỏe & tiền sử bệnh
                        </h5>
                        <p className="text-sm text-slate-700">{selectedClient.healthNotes || 'Sức khỏe bình thường'}</p>
                      </div>

                      <div className="pt-3 border-t border-slate-200">
                        <h5 className="text-xs font-bold text-indigo-600 uppercase tracking-wider mb-1 flex items-center gap-1.5">
                          <FileText className="w-4 h-4 text-[#4F46E5]" /> Ghi chú riêng của PT
                        </h5>
                        <p className="text-sm text-slate-700">{selectedClient.ptNotes || 'Chưa có ghi chú'}</p>
                      </div>

                      <div className="pt-3 border-t border-slate-200">
                        <h5 className="text-xs font-bold text-slate-700 uppercase tracking-wider mb-2 flex items-center gap-1.5">
                          <Clock className="w-4 h-4 text-indigo-600" /> Chi Tiết Lịch Sử Chỉnh Sửa, Gia Hạn & Thay Đổi Hồ Sơ ({(selectedClient.editHistory || []).length})
                        </h5>
                        {(selectedClient.editHistory || []).length === 0 ? (
                          <p className="text-xs text-slate-400 font-medium italic">Chưa có lịch sử thay đổi.</p>
                        ) : (
                          <div className="space-y-2 max-h-52 overflow-y-auto pr-1">
                            {selectedClient.editHistory?.map(item => renderHistoryEntry(item))}
                          </div>
                        )}
                      </div>
                    </div>
                  </div>
                )}

                {/* TAB 2: BODY METRICS & PROGRESS CHART */}
