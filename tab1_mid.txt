                      </div>
                    </div>

                    <div className="p-4 bg-slate-50/70 border border-slate-200 rounded-2xl space-y-4">
                      {/* Quick Observation: Check-in History & Dates */}
                      <div>
                        <div className="flex items-center justify-between mb-2">
                          <h5 className="text-xs font-bold text-emerald-700 uppercase tracking-wider flex items-center gap-1.5">
                            <Calendar className="w-4 h-4 text-emerald-600" /> Lịch Sử & Ngày Đã Check-In ({clientCheckIns.length} lượt)
                          </h5>
                          {clientCheckIns.length > 0 && (
                            <button
                              onClick={() => setActiveDetailTab('history')}
                              className="text-[11px] text-indigo-600 hover:text-indigo-800 font-bold underline flex items-center gap-1 cursor-pointer"
                            >
                              Xem tất cả lịch sử →
                            </button>
                          )}
                        </div>

                        {clientCheckIns.length === 0 ? (
                          <p className="text-xs text-slate-500 italic bg-white p-3 rounded-xl border border-slate-200">
                            Chưa có lượt check-in nào được ghi nhận cho học viên này. Bấm "Check-in 1-Chạm" ở góc trên để ghi nhận.
                          </p>
                        ) : (
                          <div className="space-y-2.5">
                            {/* Date Badges Overview */}
                            <div className="p-3 bg-white rounded-2xl border border-slate-200 shadow-2xs">
                              <p className="text-[11px] font-bold text-slate-500 uppercase tracking-wider mb-2">
                                📅 Danh sách ngày check-in gần đây:
                              </p>
                              <div className="flex flex-wrap gap-1.5 max-h-32 overflow-y-auto pr-1">
                                {clientCheckIns.slice(0, 15).map((ci) => {
                                  const dateObj = new Date(ci.timestamp);
                                  const formattedDate = dateObj.toLocaleDateString('vi-VN', {
                                    day: '2-digit',
                                    month: '2-digit',
                                    year: 'numeric'
                                  });
                                  const formattedTime = dateObj.toLocaleTimeString('vi-VN', {
                                    hour: '2-digit',
                                    minute: '2-digit'
                                  });
                                  return (
                                    <span
                                      key={ci.id}
                                      className="inline-flex items-center gap-1 text-[11px] font-extrabold bg-emerald-50 text-emerald-800 border border-emerald-200/80 px-2.5 py-1 rounded-lg hover:bg-emerald-100 transition-colors"
                                      title={`${ci.dayPlanName || 'Buổi tập'} (${formattedTime})`}
                                    >
                                      <CheckCircle2 className="w-3 h-3 text-emerald-600 shrink-0" />
                                      {formattedDate}
                                      <span className="text-[10px] text-emerald-600 font-semibold">({formattedTime})</span>
                                    </span>
                                  );
                                })}
                                {clientCheckIns.length > 15 && (
                                  <span className="inline-flex items-center text-[11px] font-bold text-slate-500 bg-slate-100 px-2 py-1 rounded-lg border border-slate-200">
                                    +{clientCheckIns.length - 15} ngày khác...
                                  </span>
                                )}
                              </div>
                            </div>

                            {/* Mini recent 3 check-ins card list */}
                            <div className="grid grid-cols-1 sm:grid-cols-3 gap-2">
                              {clientCheckIns.slice(0, 3).map((ci) => {
                                const dateObj = new Date(ci.timestamp);
                                const formattedDateTime = dateObj.toLocaleString('vi-VN', {
                                  day: '2-digit',
                                  month: '2-digit',
                                  hour: '2-digit',
                                  minute: '2-digit'
                                });
                                return (
                                  <div key={ci.id} className="p-2.5 bg-white border border-slate-200 rounded-xl text-xs flex flex-col justify-between">
                                    <div className="flex items-center justify-between font-extrabold text-slate-900 gap-1">
                                      <span className="truncate">{ci.dayPlanName || 'Check-in buổi tập'}</span>
                                      <span className="text-[10px] bg-emerald-100 text-emerald-800 px-1.5 py-0.5 rounded-md font-bold shrink-0">
                                        Trừ 1 buổi
                                      </span>
                                    </div>
                                    <p className="text-[11px] text-slate-500 font-medium mt-1 flex items-center gap-1">
                                      <Clock className="w-3 h-3 text-slate-400 shrink-0" />
                                      {formattedDateTime}
                                    </p>
                                    {ci.notes && (
                                      <p className="text-[10px] text-indigo-600 italic font-medium mt-0.5 truncate">
                                        "{ci.notes}"
                                      </p>
                                    )}
                                  </div>
                                );
                              })}
                            </div>
                          </div>
                        )}
                      </div>

                      <div className="pt-3 border-t border-slate-200">
                        <h5 className="text-xs font-bold text-indigo-700 uppercase tracking-wider mb-2 flex items-center gap-1.5">
